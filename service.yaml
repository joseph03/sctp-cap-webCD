apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: ns-app
spec:
  selector:
    app: my-app
  ports:
    - name: frontend
      port: 80
      targetPort: 80
    - name: backend
      port: 5000
      targetPort: 80